# Statistical Methods
To run the notebooks, ensure you have the following packages installed in your Python environment:
- `pandas`
- `matplotlib`
- `scikit-learn`
- `statsmodels`
- `bs4`
- `folium`

The packages can be installed by running:
   ```bash
   pip install pandas matplotlib scikit-learn statsmodels bs4 folium
